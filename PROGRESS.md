# Progress Log

## Status: In Progress

---

## Completed

### Initial Setup (2026-04-14)
- Electron + google-auth-library 설치 완료
- 기본 폴더 구조 구성 (`main.js`, `preload.js`, `renderer/`, `prompts.json`)
- Vertex AI REST API 연동 (google-auth-library 기반 OAuth 토큰 발급)
- UI 초안 구현:
  - JSON 키 입력 (파일 불러오기 / 텍스트 붙여넣기)
  - 작업 유형 선택 (번역 / 문법 교정)
  - 언어 필드 동적 표시 (교정 시 도착 언어 숨김)
  - 결과 출력 영역
  - 로딩 상태 및 에러 메시지 처리
- `prompts.json` 초안 작성 (번역 / 문법 교정 프롬프트)
- GitHub 푸시 완료

### 기능 추가 (2026-04-14)
- `prompts.json`에 `model` 필드 추가 — 관리자가 파일 수정만으로 모델 변경 가능
- `prompts.json`에 `languages` 필드 추가 (Korean, English, Japanese, Russian)
- 언어 선택 UI를 텍스트 입력에서 드롭다운으로 교체
- 드롭다운 옵션을 `prompts.json`에서 동적으로 로드하도록 구현

---

## Pending

### UI 전면 재설계
**전체 구조**
- 프레임리스 윈도우 (타이틀 바 제거)
- 좌측 사이드바 + 우측 콘텐츠 영역 구조
- 사이드바 전체가 창 드래그 영역
- 닫기 버튼(✕)을 사이드바 하단에 배치, 최소화 버튼 없음

**탭 구성 (사이드바)**
- 탭 1: 번역 / 문법 교정
- 탭 2: 설정 (Vertex AI JSON 키 입력)
- 탭 3: 미정 (전체 공간을 차지하는 레이아웃)

**탭 1 레이아웃**
- 상단 컨트롤 바 (한 줄, 최소 공간): 언어 드롭다운 + 현재 모드 표시(번역/교정) + 전송 버튼
- 하단 메인 영역: 입력창(좌) / 결과창(우) 나란히 배치, 화면 남은 공간 전부 사용
- 결과창에 복사 버튼 배치
- 결과 출력 시 자동 스크롤

**탭 2 레이아웃 (설정)**
- Vertex AI JSON 키 입력 (파일 불러오기 / 텍스트 붙여넣기)
- JSON 유효성 피드백 (로드 후 OK / 오류 표시)

**기타 UI 개선**
- 언어 드롭다운 한국어 표시 검토
- 사용자 안내 문구 추가 (출발어=도착어 → 교정, 다르면 → 번역 자동 적용 설명)
- 사용하지 않는 CSS(.radio-group) 제거

### 기타
- 프롬프트 내용 검토 및 개선
- 실제 Vertex AI 키로 동작 테스트

---

## 참고 사항

### JSON 키 휘발성
앱을 껐다 켜면 설정 탭의 JSON 키가 초기화됨. 세션 내에서는 유지되나 영구 저장 미구현. 보안상 민감한 정보이므로 저장 방식은 추후 별도 논의 필요.

### 번역 탭에서 키 준비 여부 미표시
키가 설정되지 않은 상태에서 전송 시 에러 메시지로만 안내됨. 번역 탭에서 키 준비 여부를 미리 표시하는 개선 가능.

### 기본 언어 선택
앱 실행 시 출발어 Korean, 도착어 English로 자동 설정됨. 주 사용 패턴에 따라 기본값 변경 가능.
